import XCTest
@testable import ScreenshotDetectorKit

final class ScreenshotDetectorKitTests: XCTestCase {
    func testExample() {
        // Basic sanity test to ensure the package links correctly.
        _ = ToastView(message: "Test")
    }
}


